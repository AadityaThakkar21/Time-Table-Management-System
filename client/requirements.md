## Packages
framer-motion | Smooth animations for page transitions and interactions
date-fns | Formatting timestamps for the file history
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
File upload endpoint expects multipart/form-data
Download links point to /api/files/:id/download
